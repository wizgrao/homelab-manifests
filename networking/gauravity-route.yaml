apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: default
spec:
  parentRefs:
    - name: external-gateway
      namespace: default
      sectionName: websecure
  hostnames:
    - dev.gauravity.com
  rules:
    - backendRefs:
        - name: nginx-svc
          namespace: default
          port: 8356
